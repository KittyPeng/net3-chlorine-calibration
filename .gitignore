__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Regenerable prediction caches. Too large for a git repository and reproducible from the
# scripts: see the README. The JSON result artifacts ARE tracked, because the paper quotes them.
Net3/baseline_cache/*.npy
Net3/baseline_cache/*.npz

# EPANET scratch files written next to the scripts
temp.inp
temp.rpt
temp.bin

# EPANET scratch files from step15b, which runs with file_prefix="s15b"
s15b.inp
s15b.rpt
s15b.bin
